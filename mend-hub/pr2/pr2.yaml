services:
  db:
    image: mysql:9.0
    volumes:
      - db-data:/var/lib/mysql
    networks:
      - backend

volumes:
  db-data:
    driver: local
    driver_opts:
      type: 'none'
      o: 'bind'
      device: '/data/pr2-db-data'

networks:
  frontend: {}
  backend: {}